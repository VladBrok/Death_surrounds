#include "precompiled.h"
#include "Game.h"

/**
    TODO:
    - FIX the drop down list in the settings!!!!!!!!!!
    - make the file names constant values and put them into "constants.h"
    - create a "ProgressBar" or "Bar" class and use it in PlayerGUI
 */
 
 
int main()
{
	Game game;
    game.run();
    
	return 0;
}